buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:1.0.0'
    }
}

apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'porter'
            keyPassword 'porter'
            storeFile file('/Users/inderpal/projects/personal/customerApp/key')
            storePassword 'porter'
        }
    }
    compileSdkVersion 21
    buildToolsVersion '21.1.2'
    lintOptions {
        abortOnError false
    }
    buildTypes {
        debug {
            signingConfig signingConfigs.config
        }
    }
}
repositories {
    mavenCentral()
}

dependencies {
    compile 'com.android.support:support-v4:21.0.3'
    compile 'com.google.android.gms:play-services:6.5.87'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.2.1'
}